{
  "name": "grafiter",
  "version": "1.0.0",
  "description": "GRAFITER - Tu espacio de voces",
  "scripts": {
    "start": "serve -s . -l ${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.0"
  }
}
